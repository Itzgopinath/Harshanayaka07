#include <stdio.h>

int main() {
    int i = 1;

    // do-while loop starts here
    do {
        printf("Iteration: %d\n", i); // Print the current value of i
        i++; // Increment i by 1
    } while (i <= 5); // Condition for the loop to continue

    return 0;
}
